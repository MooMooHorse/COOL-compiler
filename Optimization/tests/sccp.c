#include<stdio.h>


int main(){
    int a = 2, b =3;
    int c, d, e, f, g;
    
    // g = a + b;

    if(a < b) {
        d = a;
        c = 8;
    } else {
        e = b;
        f = 4;
    }

    return 0;

    // if(a + b >= c) return 1;

    // return 0;

}